{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "asset-resolver-file",
  "sourceRoot": "packages/asset-resolver-file/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/asset-resolver-file",
        "parallel": false,
        "commands": ["rimraf lib"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "cwd": "packages/asset-resolver-file",
        "parallel": false,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs"]
      },
      "configurations": {
        "production": {
          "env": {
            "NODE_ENV": "production"
          }
        }
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/asset-resolver-file",
        "parallel": false,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs -w"]
      }
    }
  }
}
